{% extends 'base.html.twig' %}

{% block title %}codeigniter 4 controllers{% endblock %}

	{% block meta %}
		<meta name="description" content="codeigniter 4 controllers">
		<meta name="keywords" content="codeigniter 4 controllers, codeigniter 4"/>
	{% endblock %}

	{% block stylesheets %}

{% endblock %}			

	{% block body %}
		{{ include('inc/navbar.html.twig') }}

		  <!-- Page Header-->
        <header class="masthead" style="background-image: url('../assets/theme-blog/assets/img/blog-bg.jpg')">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="site-heading">
                            <h1>codeigniter 4 controllers</h1>                            
                        </div>
                    </div>
                </div>
            </div>
        </header>   

		
		<div class="container">


		<br>
		<div class="">


		<div class="row">	
		<div class="offset-2 col-8">

		<div class="text-center">			
		<img class="img-responsive" src="../assets/img/ci4_controllers.png" alt="ci4_controllers">
		</div>
		</div>

		
		</div>
		<br>

		<div class="row">				
		<div class="offset-2 col-8 text-center">	
				

		</div>
	
		</div>		

		{# </div> #}
		<br>
		<div class="row">

		<div class="col-4">

		{# {{ include('inc/ad_defect.html.twig') }}  #}

		</div>

		<div class="offset-4 col-4">

		{{ include('subscribe/_form.html.twig') }}

		</div>


		</div>
		<hr>

		<div class="texto-blog">					
		<p>En este artículo veremos como utilizar los controladores en la versión 4 de codeigniter framework.</p>

		<h2 class="texto-title-blog">¿Cómo funcionan los controladores?</h2>

		{# {{ include('inc/ad_defect.html.twig') }}  #}
		
		<p>Primero tenemos la estructura básica de un controlador.</p>
		


		{# 	pre mantiene el formato del texto #}
		<pre class="text-code">
		<code class="">
		class Users extends BaseController
		{
		public function index()
		{		
		
		$data['tittle'] = 'Users';		

		return view('admin/index',$data);
		}

		}
		</code>
		</pre>

		<p>Los controladores se encuentran ubicados en <code>app/Controllers</code>, todo controlador extiende del controlador principal llamado BaseController, el cual se encuentra en la misma ruta.</p>

		{# <pre> #}
		<p>
		<code class="">

		class Users extends BaseController

		</code>
		</p>
		{# </pre> #}
		

		<h2 class="texto-title-blog">¿Qué es el BaseController?</h2>			

		<p>Aquí tenemos el BaseController el cual extiende de Controller que es la clase core del framework, a través de la función initController podemos cargar automáticamente servicios, librerias, modelos etc.</p>
		<pre class="text-code">
		<code class="">
		namespace App\Controllers;

		/**
		* Class BaseController
		*
		* BaseController provides a convenient place for loading components
		* and performing functions that are needed by all your controllers.
		* Extend this class in any new controllers:
		*     class Home extends BaseController
		*
		* For security be sure to declare any new methods as protected or private.
		*
		* @package CodeIgniter
		*/

		use CodeIgniter\Controller;

		class BaseController extends Controller
		{

		/**
		* An array of helpers to be loaded automatically upon
		* class instantiation. These helpers will be available
		* to all other controllers that extend BaseController.
		*
		* @var array
		*/
		protected $helpers = [];

		/**
		* Constructor.
		*/
		public function initController(\CodeIgniter\HTTP\RequestInterface $request, \CodeIgniter\HTTP\ResponseInterface $response, \Psr\Log\LoggerInterface $logger)
		{
		// Do Not Edit This Line
		parent::initController($request, $response, $logger);

		//--------------------------------------------------------------------
		// Preload any models, libraries, etc, here.
		//--------------------------------------------------------------------
		// E.g.:
		// $this->session = \Config\Services::session();
		}

		}
		</code>
		</pre>
		
		<p>En su interior tenemos una variable <code>$data</code> la cual se la podemos pasar a una vista.</p>
		<code class="">
		public function index()
		</code>

		
		<p>Ahora tenemos el método index, el cual es el primer método que se ejecuta al cargar la clase, al menos que carguemos el constructor.</p>
		<code class="">
		$data['tittle'] = 'Users';
		</code>

		
		<p>Finalmente retornamos una vista, donde el primer parámetro es el path de la vista y la cual acepta como segundo parámetro la <code>$data</code>.</p>
		<code class="">
		return view('admin/index',$data);
		</code>		


		</div>
		<br>		
	

		<blockquote >
		<p class="blockquote-footer">jonathancastro@opengiscrm.com
		</p>
		</blockquote>

		</div>




		</div>	

		</div>


		<div class="row">

		<div class="offset-8 col-4">

		<div class="">
		<a class="btn btn-outline-primary" href="/blog">
		<span class="fas fa-arrow-left"></span>		
		Regresar</a>
		</div>

		</div> 

		</div>
		<br>

		<br>
		<br>


		{{ include('inc/footer.html.twig') }} 
	{% endblock %}
