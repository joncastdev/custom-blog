{% extends 'base.html.twig' %}

{% block title %}symfony doctrine{% endblock %}

{% block meta %}
<meta name="description" content="symfony doctrine">
<meta name="keywords" content="symfony doctrine, symfony 5 doctrine"/>
{% endblock %}

{% block stylesheets %}

{% endblock %}			

	{% block body %}
		{{ include('inc/navbar.html.twig') }}

		 <!-- Page Header-->
        <header class="masthead" style="background-image: url('../assets/theme-blog/assets/img/blog-bg.jpg')">
            <div class="container position-relative px-4 px-lg-5">
                <div class="row gx-4 gx-lg-5 justify-content-center">
                    <div class="col-md-10 col-lg-8 col-xl-7">
                        <div class="site-heading">
                            <h1>symfony doctrine</h1>                            
                        </div>
                    </div>
                </div>
            </div>
        </header>    

		
		<div class="container">


		<br>
		<div class="">



		<div class="offset-2 col-8">

		<div class="text-center">			
		<img class="img-fluid" src="../assets/img/symfony_doctrine.png" alt="symfony_doctrine">
		</div>
		</div>
		</div>
		<br>

		<div class="row">				
		<div class="col-12 text-center">		

		</div>
		</div>
		<br>
		<div class="row">

		<div class="col-4">

		{{ include('inc/ad_defect.html.twig') }} 		

		</div>

		<div class="offset-4 col-4">

		{{ include('inc/sym_banner.html.twig') }} 	

		</div>


		</div>
		<hr>

		<div class="texto-blog">					
		<p>En este artículo veremos como funciona <strong>doctrine</strong> el orm de symfony 5 framework.</p>

		<h2>¿Cómo trabajar con doctrine en symfony 5?</h2>

		{{ include('inc/ad_defect.html.twig') }} 	
		
		<p>Primero tenemos que configurar el archivo <code>.env</code> ubicado en <code>nombreProyecto/.env</code>, en este ejemplo lo haremos con mysql, pero puedes usar postgresql, sqlite o lo que prefieras, recuerden que las líneas que comienzan con <strong>#</strong> son partes del código comentadas.</p>

		<pre class="text-code">
		<code>
	# In all environments, the following files are loaded if they exist,
	# the latter taking precedence over the former:
	#
	#  * .env                contains default values for the environment variables needed by the app
	#  * .env.local          uncommitted file with local overrides
	#  * .env.$APP_ENV       committed environment-specific defaults
	#  * .env.$APP_ENV.local uncommitted environment-specific overrides
	#
	# Real environment variables win over .env files.
	#
	# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
	#
	# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
	# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

	###> symfony/framework-bundle ###
	APP_ENV=dev
	APP_SECRET=97d5bfbd4870e52b7fcfb210725a567e
	###< symfony/framework-bundle ###

	###> symfony/mailer ###
	# MAILER_DSN=smtp://localhost
	###< symfony/mailer ###

	###> doctrine/doctrine-bundle ###
	# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
	# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
	#
	# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"
	# DATABASE_URL="mysql://db_user:db_password@127.0.0.1:3306/db_name?serverVersion=5.7"
	# Nos interesa esta linea para conectarnos a mysql
	  DATABASE_URL="mysql://root:123@127.0.0.1:3306/nombre_db?serverVersion=5.7"
	# DATABASE_URL="postgresql://db_user:db_password@127.0.0.1:5432/db_name?serverVersion=13&charset=utf8"
	###< doctrine/doctrine-bundle ###
		</code>
		</pre>

		<p>Luego ejecutamos el siguiente comando, en una terminal, en la raíz del proyecto, ejemplo <code>cd/nombreApp</code>,ahora nos crearía una base de datos, para nuestro ejemplo se llamaría <strong>nombre_db</strong>.</p>

		<pre class="text-code">
		<code>
	php bin/console doctrine:database:create
		</code>
		</pre>

		<h2>¿Cómo eliminar base de datos con doctrine?</h2>					

		<p>Ahora si quisiéramos eliminar esta base de datos, podemos hacer lo siguiente, y nos eliminaría la base de datos.</p>
	
		<pre class="text-code">
		<code class="">
	php bin/console doctrine:database:drop --force
		</code>
		</pre>

		<p>Estas son algunas de las muchas cosas que podemos hacer con el <strong>orm doctrine</strong>.</p>		

</div>

<br>


<blockquote >
<p class="blockquote-footer">jonathan castro
</p>
</blockquote>

</div>




</div>	

</div>


<div class="row">

<div class="offset-8 col-4">

<div class="">
<a class="btn btn-outline-primary" href="/">
<span class="fas fa-arrow-left"></span>		
Regresar</a>
</div>

</div> 

</div>
<br>

<br>
<br>


{{ include('inc/footer.html.twig') }} 
{% endblock %}
